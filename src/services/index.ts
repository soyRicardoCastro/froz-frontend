import { getTask, getTasks } from './tasks'
import { getUnis, getUni } from './unis'
import { login, register } from './auth'
import { getUser, getUsers } from './users'

export {
  getTask,
  getTasks,
  getUnis,
  getUni,
  login,
  register,
  getUser,
  getUsers
}
